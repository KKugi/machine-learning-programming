안녕하세요

git
git --version
git config --global user.name(    )
git config --global user.email(    )
cd C:\(경로)
git clone http~~~~~
cd C:\(경로)
git add test.txt
git commit -m "Add Test file [test.txt]"
git push